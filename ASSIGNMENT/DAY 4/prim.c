# include<stdio.h>
int crp(int);
int main()
{
    int no=0;
    scanf("%d",&no);
    printf("sum of digits of %d is %d",no,sod(no));
    return 0;
}
int cpr(int n1)
{
    if(i==1)
    return 1;
}
else if(n1%1==0)
{
    return 0;
}else
{ 
    i=i-1;
    cpr(n1);
}